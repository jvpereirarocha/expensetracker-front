version: '3.8'

services:
  expense-tracker:
    build: .
    ports:
      - '3000:80'
    restart: always
    container_name: expense-tracker
    extra_hosts:
      - 'host.docker.internal:host-gateway'
